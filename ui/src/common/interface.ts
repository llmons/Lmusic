export interface Song {
  name: string;
  artist: string;
  pic: string;
  url: string;
  lrc: string;
}

export interface SimpleSong {
  id: string;
  name: string;
  artist: string;
  picurl: string;
}
